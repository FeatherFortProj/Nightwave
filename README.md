# Nightwave Android Redirect - EOS

Nightwave Android Redirect is an EOS redirect and SSL bypass tool for Fortnite, designed to work with Fortnite versions **22 – latest _(probably)_**. It hooks into **ProcessRequest** an **Unreal Engine Function** that is responsible for processing EOS's HTTP requests and redirects them to custom backends.